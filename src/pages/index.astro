---
import Layout from '../layouts/Layout.astro';
import productsData from '../../data.json';

// Načtení a seřazení produktů
const cakes = (productsData?.cakes || []).sort((a, b) => (a.weight ?? 999) - (b.weight ?? 999));
const desserts = (productsData?.desserts || []).sort((a, b) => (a.weight ?? 999) - (b.weight ?? 999));

// Definice prodejen
const stores = [
	{ id: 'petrvald', name: 'Petřvald', address: 'Šenovská 1', phone: '+420 778 157 857', closed: [1], hours: 'Út-Ne: 9:00 - 17:00' },
	{ id: 'pist', name: 'Píšť', address: 'Opavská 218/101', phone: '+420 602 323 788', closed: [0, 6], hours: 'Po-Pá: 6:00 - 15:00' },
	{ id: 'karvina', name: 'Karviná', address: 'tř. Těreškovové 2233/28', phone: '+420 778 157 867', closed: [0], hours: 'Po-Pá: 9:00 - 17:00, So: 8-12' },
	{ id: 'ostrava', name: 'Ostrava (Zábřeh)', address: 'Výškovická 116a', phone: '+420 775 271 101', closed: [0], hours: 'Po-Pá: 9:00 - 17:00, So: 9-12' }
];
---

<Layout title="Cukrářství Blahutovi - Tradiční výroba">
	<style is:global>
		:root { --navy: #0a192f; --gold: #d4af37; --offwhite: #fcfaf7; }
		body { background-color: var(--offwhite); font-family: 'Plus Jakarta Sans', sans-serif; margin: 0; scroll-behavior: smooth; color: var(--navy); }
		
		/* UI Prvky */
		header { background: var(--navy); padding: 1.2rem 0; color: white; position: sticky; top: 0; z-index: 100; border-bottom: 2px solid var(--gold); }
		.nav-container { max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; padding: 0 1rem; }
		.nav-links a { color: white; text-decoration: none; margin-left: 1.2rem; font-weight: bold; font-size: 0.85rem; text-transform: uppercase; }
		
		.section-title { text-align: center; font-family: 'Playfair Display', serif; font-size: 2.5rem; margin: 4rem 0 2rem; }
		.grid { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; padding: 0 1rem; }
		
		.card { background: white; border-radius: 12px; overflow: hidden; box-shadow: 0 5px 15px rgba(0,0,0,0.05); border: 1px solid rgba(212,175,55,0.1); text-align: center; }
		.card img { width: 100%; height: 280px; object-fit: cover; }
		
		.btn-add { background: var(--gold); color: var(--navy); padding: 0.8rem 1.5rem; border-radius: 50px; font-weight: bold; border: none; cursor: pointer; display: inline-block; text-decoration: none; transition: 0.2s; }
		.btn-add:hover { background: #c19b2e; }

		.input-field { padding: 0.8rem; border-radius: 8px; border: 1px solid #ddd; width: 100%; box-sizing: border-box; font-family: inherit; margin-top: 5px; }

		/* Košík ikona */
		#cart-float { position: fixed; bottom: 30px; right: 30px; background: var(--gold); color: var(--navy); width: 65px; height: 65px; border-radius: 50%; display: flex; align-items: center; justify-content: center; box-shadow: 0 10px 25px rgba(0,0,0,0.2); cursor: pointer; z-index: 1000; }
		#cart-count { position: absolute; top: 0; right: 0; background: var(--navy); color: white; width: 22px; height: 22px; border-radius: 50%; font-size: 0.75rem; display: flex; align-items: center; justify-content: center; font-weight: bold; }

		/* Modály */
		#cart-modal { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.8); z-index: 2000; align-items: center; justify-content: center; padding: 15px; }
		.modal-content { background: white; width: 100%; max-width: 500px; border-radius: 20px; padding: 2rem; max-height: 90vh; overflow-y: auto; position: relative; }
	</style>

	<header>
		<div class="nav-container">
			<h1 style="margin:0; color:var(--gold); font-family: 'Playfair Display'">Cukrářství Blahutovi</h1>
			<nav class="nav-links">
				<a href="#dorty">Dorty</a>
				<a href="#zakusky">Zákusky</a>
				<a href="#dort-na-prani">Na přání</a>
				<a href="#b2b">B2B</a>
				<a href="#prodejny">Prodejny</a>
			</nav>
		</div>
	</header>

	<div id="cart-float" onclick="toggleModal(true)">
		<svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>
		<div id="cart-count">0</div>
	</div>

	<div id="cart-modal">
		<div class="modal-content">
			<button onclick="toggleModal(false)" style="position:absolute; right:20px; top:20px; border:none; background:none; font-size:1.5rem; cursor:pointer">✕</button>
			<h2 style="font-family:'Playfair Display'; margin-top:0">Vaše rezervace</h2>
			<div id="cart-items" style="margin: 1.5rem 0; border-bottom: 1px solid #eee; padding-bottom: 1rem;"></div>
			
			<form name="objednavka-kosik" method="POST" data-netlify="true" id="order-form">
				<input type="hidden" name="seznam-zbozi" id="hidden-cart-details" />
				<input type="hidden" name="vyzvednuti-prodejna" id="hidden-store" />
				<input type="hidden" name="vyzvednuti-datum" id="hidden-date" />

				<div style="margin-bottom:1rem">
					<label style="font-size:0.8rem; font-weight:bold;">Místo vyzvednutí</label>
					<select id="store-select" class="input-field" required onchange="validateDate()">
						<option value="">Vyberte prodejnu...</option>
						{stores.map(s => <option value={s.id} data-closed={s.closed.join(',')}>{s.name}</option>)}
					</select>
				</div>
				<div style="margin-bottom:1rem">
					<label style="font-size:0.8rem; font-weight:bold;">Datum vyzvednutí (min. za 3 dny)</label>
					<input type="date" id="date-input" class="input-field" required onchange="validateDate()" />
					<p id="date-error" style="color:red; font-size:0.75rem; display:none; margin-top:5px;">V tento den máme zavřeno nebo je termín příliš brzy.</p>
				</div>
				<input type="text" name="jmeno" id="cust-name" placeholder="Celé jméno" class="input-field" required style="margin-bottom:0.8rem" />
				<input type="tel" name="telefon" id="cust-phone" placeholder="Telefon" class="input-field" required />
				<button type="submit" id="submit-btn" class="btn-add" style="width:100%; margin-top:1.5rem">Odeslat objednávku</button>
			</form>
		</div>
	</div>

	<section id="dorty">
		<h2 class="section-title">Naše dorty</h2>
		<div class="grid">
			{cakes.map((p) => (
				<div class="card">
					<img src={p.foto} alt={p.title} loading="lazy" />
					<div style="padding: 1.5rem">
						<h3 style="margin:0; font-family:'Playfair Display'">{p.title}</h3>
						<p style="font-size:0.8rem; color:#888;">{p.porce}</p>
						<p style="color:var(--gold); font-weight:bold; font-size:1.2rem;">{p.cena}</p>
						<button class="btn-add" onclick={`addToCart('${p.title}', '${p.cena}')`}>Do košíku</button>
					</div>
				</div>
			))}
		</div>
	</section>

	<section id="zakusky" style="background: white; padding-bottom: 5rem;">
		<h2 class="section-title">Naše zákusky</h2>
		<div class="grid">
			{desserts.map((p) => (
				<div class="card">
					<img src={p.foto} alt={p.title} loading="lazy" />
					<div style="padding: 1.5rem">
						<h3 style="margin:0; font-family:'Playfair Display'">{p.title}</h3>
						<p style="color:var(--gold); font-weight:bold; font-size:1.2rem;">{p.cena}</p>
						<button class="btn-add" onclick={`addToCart('${p.title}', '${p.cena}')`}>Do košíku</button>
					</div>
				</div>
			))}
		</div>
	</section>

	<section id="dort-na-prani" style="padding: 5rem 1rem; background: #f0ede9;">
		<div style="max-width: 750px; margin: 0 auto; background: white; padding: 3rem; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.05);">
			<h2 style="text-align: center; font-family: 'Playfair Display'; font-size: 2.5rem; margin-bottom: 1rem;">Dort na přání</h2>
			<p style="text-align: center; margin-bottom: 2rem; opacity: 0.8;">Máte vlastní představu? Popište nám ji a nahrajte obrázek pro inspiraci.</p>
			
			<form name="dort-na-prani" method="POST" data-netlify="true" enctype="multipart/form-data">
				<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-bottom: 1rem;">
					<input type="text" name="jmeno" placeholder="Vaše jméno" class="input-field" required />
					<input type="tel" name="telefon" placeholder="Váš telefon" class="input-field" required />
				</div>
				<textarea name="popis" placeholder="Detaily o dortu (příchuť, téma, počet porcí...)" class="input-field" style="height: 120px; margin-bottom: 1rem;" required></textarea>
				<label style="display: block; margin-bottom: 0.5rem; font-weight: bold; font-size: 0.85rem;">Inspirační obrázek (formát JPG/PNG):</label>
				<input type="file" name="priloha-foto" accept="image/*" class="input-field" style="margin-bottom: 1.5rem; padding: 0.5rem;" />
				<button type="submit" class="btn-add" style="width: 100%; margin: 0;">Odeslat poptávku na míru</button>
			</form>
		</div>
	</section>

	<section id="b2b" style="background: var(--navy); color: white; padding: 5rem 1rem; text-align: center;">
		<div style="max-width:1000px; margin:0 auto;">
			<h2 style="color:var(--gold); font-family:'Playfair Display'; font-size:2.8rem; margin-bottom: 2rem;">Velkoobchodní spolupráce</h2>
			<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; margin-bottom: 3rem; text-align: left;">
				<div style="background: rgba(212,175,55,0.1); padding: 2rem; border-radius: 15px; border: 1px solid var(--gold);">
					<h3 style="color:var(--gold); margin-top:0;">Výhodnější ceny</h3>
					<p>Získejte speciální velkoobchodní ceník pro vaši kavárnu, restauraci nebo obchod.</p>
				</div>
				<div style="background: rgba(255,255,255,0.05); padding: 2rem; border-radius: 15px; border: 1px solid rgba(255,255,255,0.1);">
					<h3 style="color:var(--gold); margin-top:0;">Závozy</h3>
					<p>Zavážíme těmito vozy: <strong>Karviná, Ostravsko, Opavsko, Havířov, Bohumín.</strong></p>
				</div>
			</div>

			<div style="background: white; color: var(--navy); padding: 2.5rem; border-radius: 15px; max-width: 550px; margin: 0 auto; text-align: left;">
				<h3 style="text-align: center; font-family:'Playfair Display'; margin-top:0;">Poptat spolupráci</h3>
				<form name="b2b-poptavka" method="POST" data-netlify="true">
					<input type="text" name="firma" placeholder="Název firmy / Jméno" class="input-field" required style="margin-bottom:0.8rem" />
					<input type="tel" name="telefon" placeholder="Telefon" class="input-field" required style="margin-bottom:0.8rem" />
					<textarea name="poznamka" placeholder="Lokalita provozovny a zájem o sortiment..." class="input-field" style="height: 80px; margin-bottom: 1.5rem;"></textarea>
					<button type="submit" class="btn-add" style="width: 100%; margin: 0;">Zaslat žádost o ceník</button>
				</form>
				<p style="text-align: center; margin-top: 1.5rem; font-weight: bold; font-size: 0.9rem;">Tel: +420 602 323 788</p>
			</div>
		</div>
	</section>

	<section id="prodejny" style="padding: 5rem 1rem;">
		<h2 class="section-title">Naše prodejny</h2>
		<div class="grid">
			{stores.map((s) => (
				<div class="card" style="padding: 2rem; background: var(--navy); color: white; text-align: left;">
					<h3 style="color:var(--gold); margin-top:0; font-family:'Playfair Display'; font-size:1.6rem;">{s.name}</h3>
					<p>📍 {s.address}</p>
					<p>📞 {s.phone}</p>
					<p style="margin-top: 15px; font-size: 0.85rem; opacity: 0.7;">{s.hours}</p>
				</div>
			))}
		</div>
	</section>

	<footer style="background: #050c18; color: white; text-align: center; padding: 4rem 1rem;">
		<p>© 2026 Cukrářství Blahutovi | Tradiční receptury</p>
	</footer>

	<script is:inline>
		let cart = [];
		const dateInput = document.getElementById('date-input');
		const minDate = new Date();
		minDate.setDate(minDate.getDate() + 3);
		dateInput.min = minDate.toISOString().split('T')[0];

		function addToCart(title, price) {
			const existing = cart.find(i => i.title === title);
			if (existing) { existing.qty++; } else { cart.push({ title, price, qty: 1 }); }
			updateCart();
			alert('Přidáno do košíku');
		}

		function updateCart() {
			const countEl = document.getElementById('cart-count');
			const itemsEl = document.getElementById('cart-items');
			countEl.innerText = cart.reduce((sum, item) => sum + item.qty, 0);
			if (cart.length === 0) { itemsEl.innerHTML = '<p style="text-align:center; opacity:0.4;">Košík je prázdný.</p>'; return; }
			itemsEl.innerHTML = cart.map((item, index) => `
				<div style="display:flex; justify-content:space-between; margin-bottom:10px; font-size:0.9rem">
					<span><b>${item.qty}x</b> ${item.title}</span>
					<span>${item.price} <button onclick="removeItem(${index})" style="color:red; border:none; background:none; cursor:pointer;">✕</button></span>
				</div>
			`).join('');
		}

		function removeItem(index) { cart.splice(index, 1); updateCart(); }
		function toggleModal(show) { document.getElementById('cart-modal').style.display = show ? 'flex' : 'none'; }

		function validateDate() {
			const storeSelect = document.getElementById('store-select');
			const errorEl = document.getElementById('date-error');
			const submitBtn = document.getElementById('submit-btn');
			if (!dateInput.value || !storeSelect.value) return;
			const day = new Date(dateInput.value).getDay();
			const closed = storeSelect.options[storeSelect.selectedIndex].dataset.closed.split(',').map(Number);
			const isInvalid = closed.includes(day);
			errorEl.style.display = isInvalid ? 'block' : 'none';
			submitBtn.disabled = isInvalid;
			submitBtn.style.opacity = isInvalid ? '0.4' : '1';
		}

		document.getElementById('order-form').onsubmit = function(e) {
			if (cart.length === 0) { e.preventDefault(); return alert('Košík je prázdný!'); }
			const storeName = document.getElementById('store-select').options[document.getElementById('store-select').selectedIndex].text;
			document.getElementById('hidden-cart-details').value = cart.map(i => `${i.qty}x ${i.title} (${i.price})`).join('\n');
			document.getElementById('hidden-store').value = storeName;
			document.getElementById('hidden-date').value = dateInput.value;
			// Netlify Forms se postará o zbytek po odeslání.
		};
	</script>
</Layout>
